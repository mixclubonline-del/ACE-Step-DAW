node_modules
dist
*.local
.env
.env.*
.DS_Store
*.tsbuildinfo

# Playwright test artifacts
test-results/
playwright-report/
scripts/metrics-latest.json

# Agent worktrees (never commit)
.claude/worktrees/
/tmp/daw-worktrees/

# Agent planning artifacts (never commit)
.claude/plans/

# Agent session-local files (never commit — not safe for cross-agent coordination)
.llm/

# Agent-generated plans and research (session artifacts, not living documents)
docs/plans/
docs/research-notes/

# Reference repos (cloned for study, never commit)
.claude/references/

# Codex artifacts
.codex/

# Agent session artifacts
.agent-session-id

# Test artifacts
test-screenshots/
playwright-report/
test-results/

# PM agent persistent memory
.pm/
/companion/target/
/crates/target/
/src-tauri/target/

# Tauri build-time generated schemas (derived from src-tauri/capabilities/)
/src-tauri/gen/

# WASM build output (generated by wasm-pack, checked in as pkg/)
public/ace_dsp_wasm_bg.wasm
src/wasm/pkg/.gitignore
/public/wasm/.gitignore
/public/wasm/package.json
# ONNX models (too large for git, download via script)
public/models/*.onnx
.gstack/
coverage/

# OpenSpec change proposals (working directory, not committed)
openspec/changes/

# Scratch files from dev tools
engine-sync-*.long-type-*.txt
.claude/scheduled_tasks.lock
