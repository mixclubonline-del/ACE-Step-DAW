# ACE-Step DAW — Agent Context

## Identity
You are a developer for ACE-Step DAW, a browser-based AI-native DAW.

## Must-Read Before Coding
- `CLAUDE.md` — interaction design standards, quality gates
- `AGENTS.md` — dev process, PR workflow, competitive research depth

## Development SOP (mandatory)

### 1. Test-Driven Development
- Write a failing test FIRST
- Then implement the minimum code to pass
- Then refactor
- Every feature needs: unit test + build passes

### 2. UX/UI Standards (from CLAUDE.md)
- Visual feedback within 100ms
- Keyboard shortcut for every mouse action
- Progressive disclosure: simple by default
- Drag operations need data-testid attributes
- Color-blind safe: never use color alone
- Components < 600 lines

### 3. Code Quality
- 0 TypeScript `any` types
- Every UI action = Zustand store action
- Undo support: every state change calls _pushHistory()
- Git identity: ChuxiJ <junmin@acestudio.ai>

### 4. Before Committing
- npx tsc --noEmit (0 errors)
- npm run build (success)
- npx vitest run tests/unit/ (all pass)

### 5. PR Standards
- Title: "feat: #NUMBER — description" or "fix: #NUMBER — description"
- Body: "Closes #NUMBER"
- Must include tests for new functionality
- Branch: fix/issue-NUMBER

### 6. You Own the PR Until It Merges
- Your job is NOT done when you create a PR — you own it until it merges.
- After your initial implementation, the system will wait for CI and code reviews.
- If CI fails or reviewers leave comments, you will be **resumed in the same session**
  with full context of your prior work. No need to re-read files you already know.
- You MUST fix every CI failure and address every review comment — do not skip any.
- This may happen multiple times. Each round, fix all issues and pass quality gates.
- If you cannot resolve an issue, explain why clearly in your commit message.
---
IMPLEMENT ISSUE #438: feat: add a dedicated AI generation control panel
## User Story
As a user, I want a dedicated AI generation panel with prompt, style, key, BPM, length, temperature, and variation controls, so that I can generate musical ideas without hidden settings.

## Source
Unchecked item from `docs/design/UX_IMPROVEMENT_CHECKLIST.md`:
- P1 AI Generation UX: **Generation panel**

## Problem
AI generation controls are not consolidated into a single, explicit workflow surface. The checklist calls for a structured sidebar so users can set generation intent before running requests.

## Scope
Create a generation sidebar panel for core prompt and generation parameters.

## Acceptance Criteria
- The panel includes controls for prompt text, style tags, key, BPM, length, temperature, and variation count.
- The panel state is accessible via typed Zustand actions and readable from `window.__store`.
- Controls have ARIA labels and keyboard-focus support.
- Reasonable defaults are provided so generation can start without excessive setup.
- The design fits the existing DAW layout without obscuring core transport/timeline interactions.
- Browser testing covers opening the panel, changing settings, triggering generation, and verifying submitted parameters.

## Verification
- `npx tsc --noEmit`
- `npm run build`
- Browser test: configure the panel, run generation, and confirm the request payload/state matches the selected parameters.


You are on branch fix/issue-438. Implement, then: npx tsc --noEmit && npm run build && npx vitest run tests/unit/ && git add -A && git commit -m 'feat: resolve #438'. Do NOT push.
